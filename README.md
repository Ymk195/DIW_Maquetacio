# Restaurant els coberts
## Branca: multimedia
## Ubicacio de les transicions i animacionsç

### TRANSICIONS (Exercici 1)
Nº  -   Descripció - Fitxer - Selector
1.1 -   Galeria d'imatges – hover activa `scale`, `rotate` i `grayscale` - `style.css` - `.flex_galeria img:hover`
1.1 -   Plats de la carta – hover activa `scale`, `rotate` i `grayscale` - `style.css` - `.plato img:hover`
1.2 -   Botó "Reservar" fix – hover canvia `background-color`, `padding` i `border-radius` (3 propietats) - `style.css` - `.reserva_fixe:hover`
4.3 -   Icones control vídeo – hover activa `text-shadow` i `color` - `style.css` - `.icona_video:hover` 
5.1 -   Navbar shrink – scroll afegeix classe `.petit` que transiciona `padding` i `background-color`  `style.css`  `header` + JS scroll

### ANIMACIONS (Exercici 2)
Nº  -   Descripció - Fitxer - Keyframe / Selector
2.1 -   Text hero – apareix pujant de baix, color taronja i color final. 4 estats: `opacity`, `transform`, `color` - `style.css` - `@keyframes apareix` i `.texte_hero h1, .texte_hero p`
2.2 -   Pulsació botó reservar – bucle infinit amb 4 frames, canvia `background-color`, `transform` i `box-shadow` - `style.css` - `@keyframes pulsacio` i `.reserva_fixe`
